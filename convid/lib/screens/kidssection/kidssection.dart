import 'package:flutter/material.dart';

import 'components/body.dart';

class KidsSectionScreen extends StatelessWidget {
  static String routeName = "/kidssection";
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Body(),
    );
  }
}
